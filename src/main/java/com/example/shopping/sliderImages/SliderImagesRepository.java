package com.example.shopping.sliderImages;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SliderImagesRepository extends JpaRepository<SliderImages, String> {


//    List<Categories> findAllCategories(Pageable pageRequest);

}
